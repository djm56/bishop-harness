# Local, machine-specific Claude Code settings (never commit)
settings.local.json

# OS / editor cruft
.DS_Store
__MACOSX/

# Transient agent working memory (archived per mission; keep the folder and its README)
memory/workspace/*
!memory/workspace/.gitkeep
!memory/workspace/README.md
