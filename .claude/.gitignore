# Local, machine-specific Claude Code settings (never commit)
settings.local.json

# OS / editor cruft
.DS_Store
__MACOSX/

# Transient agent working memory (archived per task; keep the folder and its README)
memory/agent-documents/*
!memory/agent-documents/.gitkeep
!memory/agent-documents/README.md
